# 质量审计提示词库 v2

> v1（5.5pro 生成版）已整体归档至 `archive-v1/`。
> v2 重构要点：21 主题合并为 15；每个主题带本仓库真实起点锚点和已知业务语义（防误报）；`_base` 强制"读过代码才能报告"+ 数量纪律；新增运行记录表跟踪验证状态。

## 使用方式

- **Claude Code（推荐）**：`/audit <主题>` 自动拼接执行，如 `/audit money`、`/audit arch A`、`/audit concurrency apps/gateway/`。审计完成后更新下方运行记录表。
- **Codex `/goal`**：手动拼接 `_base.md` + 一个主题文件。
- 纪律：一次只跑一个主题；在干净会话中跑；限定范围时在主题前加一行 `只扫描 <路径>`。
- `mode-*` 独立模式不拼接 `_base.md`。

## 文件索引

### 基础

| 文件 | 用途 |
|---|---|
| `_base.md` | 审计纪律 + 严重度 + 误报过滤 + 已知项目语义 + 输出格式（每次必拼） |

### 主题（按优先级排列）

| 文件 | 用途 |
|---|---|
| `money-settlement.md` | 金额精度/溢出/超扣 + 结算重跑幂等与 stat_date 口径（资损优先） |
| `consistency-atomicity.md` | 事务边界 + 幂等 + 半成功状态 + 异步任务 |
| `consistency-state.md` | 状态机 + 业务不变量 + MySQL/Redis 投影漂移 |
| `realtime-messaging.md` | 长连接生命周期 + 投递语义 + 在线状态（IM 核心） |
| `security-authz.md` | 入口鉴权 + 对象级越权（HostUserID 语义） |
| `concurrency-resource.md` | 并发竞态 + 资源泄漏（ws 连接表 / roomactor 边界） |
| `error-boundary.md` | 错误吞噬 + nil/零值边界 |
| `resilience-overload.md` | 超时/重试/降级 + 限流/背压/资源耗尽 |
| `contract-alignment.md` | API/字段语义/时间口径/Schema 三层对齐 |
| `perf-capacity.md` | 性能退化 + SQL/索引（MySQL 5.7） |
| `security-exposure.md` | 敏感信息泄漏 + 隐私 + 供应链 |
| `release-ops.md` | 发布兼容 + 迁移 + 脚本与启动安全 |
| `observability.md` | 排障链路缺口（按工单演练法） |
| `test-effectiveness.md` | 测试能否拦住重要回归 |
| `debt-cleanup.md` | Dead code + 重复逻辑 + 低价值抽象 |
| `counterfactual-baseline.md` | 反事实最小解审查：对治理/修复机制构造更短候选 + 失败矩阵，扫"被更简等价方案支配"的劣化实现 |

### 独立模式

| 文件 | 用途 |
|---|---|
| `mode-change-review.md` | 变更 Review（PR/diff 增量，要求读 diff 外完整上下文） |
| `mode-over-implementation.md` | 过度实现 Review（diff 相对最小充分解是否做多了：投机性扩面/过度防御/抽象/配置/测试/注释） |
| `mode-entry-map.md` | 高风险入口识别，产物落盘 `entry-map.md` 可增量刷新 |
| `mode-architecture-review.md` | 架构师定期评审（账本 `arch-reports/_ledger.md`，证据驱动轮换维度） |

## 推荐节奏

- **首次**：`mode-entry-map` 产出地图 → 按主题表顺序跑前 5 个
- **月度**：跑 2-3 个主题，选择依据：近期改动集中区 + 运行记录里最久未跑的
- **PR**：`mode-change-review`，可追加一个主题深挖
- **发布前**：`release-ops` → `security-exposure`
- **架构**：每 1-2 个月 `/audit arch`，按 `mode-architecture-review.md` 的账本机制走

## 运行记录

> 每次审计后更新本表。这张表同时是提示词的验证记录：跑完若发现提示词本身的问题（误报多、范围不清、规则被无视），记在备注里并回改提示词。

| 主题 | 最近运行 | 发现 (P0/P1/P2/P3) | 备注（含提示词回改记录） |
|---|---|---|---|
| money-settlement | 2026-06-22 | 0/1/1/1 | 06-22 复核：旧 P1 earning_daily 残留键/公会组键漂移、旧 P2 profile 失败键坍缩/公会归属漂移、旧 P3 upsert 失败静默均按 06-15 复核结论保持已修。旧“扣费成功但订单被 stale-close”表述需校准：当前 CloseStaleGiftActions/CloseRoomPendingGiftActions 只关 pending，claim 后 charging 单由 GiftReconcileJob 查钱包终态，paid/failed 会回写本地。已修 P1-1：房间礼物 `processGiftSubmit` 在 paid 后 `ApplyGiftHeat` 保存失败时不再返回 error，而是记录错误后继续返回成功并构造礼物 event/income patch/IM 通知；新增 `TestProcessGiftSubmitKeepsSuccessWhenApplyGiftHeatFailsAfterPayment`。部分缓解 P1-2：coupon 发券外部 backend 成功后，本地 `issue:<logID>` action_log 写入已从 best-effort 改为成功回包/发 IM 前的强边界；`FirstPayCouponIncomeConsumer` 缺 issue action 时也会返回错误，避免 MQ 静默消费完成。仍 open P1-2 残余：外部 `SendCoupon` 请求无 requestId 幂等或 outbox/reversal，外部成功后本地落库失败仍可能留下孤儿券，客户端重试还可能重复发券。仍 open P2：自动对账 5 次 unknown/异常后只置 `manual_reconcile`，系统无退款/冲正/继续低频补偿入口，需人工闭环。新增 P3：paid 后 heat 保存失败当前只打日志，没有 heat patch 补扫/重放。已修 P2：当前 worktree 增加 count/targets 上限和金额、收益、热度 checked 计算，补齐房间/私聊礼物 `afterBalance + totalAmount` 余额快照溢出检查，并用 `AddVoiceRoomHeatScore` 饱和加法避免房态累计热度接近 MaxInt64 时 wrap；`AppendSignalEvent` 已改审计 best-effort，不再阻断 paid 后礼物响应。检查通过：支付 claim 行锁+状态机、stale close 不关闭 charging、paid/failed 对账终态不继续累计 retry；私聊礼物 paid 后 IM 失败会标 target failed 并返回成功结果。 |
| consistency-atomicity | 2026-06-22 | 0/0/2/2 | 06-22 复核旧 open 项并扩展同类入口：已修 P1-1，房态 mutation 成功后 `AppendSignalEvent` 失败阻断 ack/dispatch 的问题已由当前 worktree 改为审计 best-effort，并补 join/blacklist 失败注入测试。已修 P1-2：`ProcessSignal` 成功后的 close-room summary、active_room/presence-cleanup 失败不再阻断已落地信令返回，router 改为错误日志后继续构造 ack/dispatch，active_room/presence cleanup 改为聚合错误并继续批处理；RTC grant refresh 改为 mutation 时携带 channel/发布权限，构造 dispatch 时不再后置回读 RoomState，BuildRoomToken 失败仍是硬业务边界；新增 close summary 与 active_room/presence cleanup 失败注入测试。部分修复并降级旧 P1-3：房间管理持久态/Redis 房态双写半成功仍覆盖房管授予/撤销/邀请接受、RemoveRoomAdmin、房内拉黑，SetOwned/SaveSnapshot 失败仍不会回滚已提交的 MySQL，但当前 worktree 已在 manager 热路径用 `HasActiveRoomAdmin` 重查 DB，并在普通房间信令与 `gift/submit` 扣款前用 `IsAnchorBlacklisted` 预检，旧 admin 或已拉黑用户不能再仅凭旧 Redis snapshot 继续行动/送礼；残余为 P2 投影/补偿风险。P2 文字禁言仍先写 Redis 房态再写 MySQL，DB 失败会让客户端 textMuted 与融云回调 DB 判定冲突。P3-1 房间/私聊礼物 IM 发送成功后 MarkActionTargetNotified 失败仍只打日志，未见 notify_status=pending/partial 补扫 job。P3-2 信令后置 close summary/active_room/presence 清理失败当前只日志降级，未见统一指标或补偿队列。检查通过：roomactor 串行化和 owner fencing 仍在；RevokeRoomAdmin/RevokeRoomTextMute 条件更新会检查 affected rows；Agora 配置启动期校验已存在；GetUserInfos enrich 失败已是日志后继续。原 06-15 记录还包括礼物 action/play/settlement 创建事务、支付 claim 行锁+幂等 order_no、reconcile bounded retry、任务奖励/首充/游戏流水唯一 source upsert 检查通过。 |
| consistency-state | 2026-06-22 | 0/0/0/0 | 06-22 复核：旧 P3 主播 presence 创建 best-effort 导致关房薪资时长永久丢失已消除，`ClosePresencesByRoomAndEnqueueAnchorLiveSettle` 当前同一事务内先按 `sl_voice_room.anchor_user_id` 回填缺失主播 presence，再 close presences，再 enqueue settle；已有缺失回填/已存在不重复/非正时长跳过测试。旧人工确认 host 判定：stale reconcile 候选现在优先用 MySQL `anchor_user_id`，仅缺失时 fallback effective host；presence cleanup 仍按 active Redis snapshot `HostUserID` 做恢复窗口业务动作，当前不是确认漂移，未来 creator/host 分离需专项回归。检查通过：heartbeat flush 成功更新后才删 Redis heartbeat，MySQL 更新只前进且不重开已离房 presence；`go test ./apps/app-service/internal/job -run 'Test(StalePresence|PresenceHeartbeat)'` 通过，完整 job 包仅因 sandbox 禁止 miniredis bind 失败。提示词运行正常，无回改。 |
| realtime-messaging | 2026-06-22 | 0/0/0/1 | 06-22 复核：旧 3×P2 均已按当前代码证伪/修复：关房 dispatch 后 cleanupClosedRoomAfterDelivery/closeLocalRoom 会 hub.CloseRoom 并批量 RemoveRoomConnSubscriptions；resume 通过 filterResumableRooms/isResumableRoomState 重新拉房态并要求 active+member，失败 fail-closed；join barrier 先 fetchRoomStateSync，sendMu 只包入队/回放，不再持锁 RPC。仍 open P3：pong handler 在 RefreshConnectionTTL 失败时 `return err`，Redis 瞬时写失败会导致健康 WS read loop 失败并走断开清理。旧 P3-2/P3-3 本轮判定已修复/缓解：kick/block 走 user-scope PushRoomKicked/PushAnchorBlacklisted，gateway applyRoomRemovalDispatch 调 LeaveUserRoom 按 userID 清本节点所有连接订阅；pending 128 虽仍大于默认 send queue 64，但 overflow/version gap 会刷新全量 sync，send queue 满显式 conflict/关闭，不是静默乱序。 |
| security-authz | 2026-06-22 | 0/0/0/1 | 06-22 复核：旧 P1 `requireVoiceRoomAnchor` 调试短路在当前工作区已消除，helpers.go 会调用 RoomApp.IsVoiceRoomAnchor，非主播返回 OnlyAnchorCanAccess；`git status --short` 只剩审计文档变更，未见 helpers.go 残留。检查通过：公网 `/v1` 业务路由统一挂 AuthMiddleware 并从 claims 取用户，不信 body uid；`/internal` 路由挂 InternalHTTPAuthMiddleware；WS 建连 uid+skey 鉴权后每条信令下传 claims.UserID，客户端 envelope.FromUserID 不作为授权来源；融云 callback 校验 signature/appKey/timestamp/replay；房间数据面板用 EffectiveHostUserID(room.HostUserID,room.CreatorUserID) 授权。新增 P3：客户端可伪造正数 envelope.FromUserID，caller-only ack/error 会回显该值；授权和房间广播仍使用 claims.UserID，不构成当前越权，但会误导信任 ack 字段的客户端日志/UI。人工确认：未来 creator/host 正式分离时需复核 roomshare AnchorUserID 写 creator、部分实时路径直接比较 snapshot.HostUserID 的语义。 |
| concurrency-resource | 2026-06-22 | 0/0/0/0 | 06-22 复核未发现确认问题。检查范围：WS 单连接 writePump/入队/close channel 由 sendMu+atomic closed 串行化，未见并发写 websocket 或 send-on-closed；dispatch Pub/Sub/stream consumer 与 node heartbeat 均监听 signal ctx，pubsub/ticker 有关闭，stream read 失败会 retry+recover pending；roomactor mutation 单 goroutine 串行，reply channel 1 缓冲避免调用方超时后 actor 写回阻塞，renew/idle timer 有 Stop，WithTimeout 有 cancel；job.Ticker 具备 per-run timeout、panic recover、ticker/timer Stop；roomshare attributor Submit/Close 用 RWMutex 避免 close 后 send，worker range channel，单任务 500ms timeout。旧确认不修项仍维持：ServiceContext.Close 不等待 actor done 只是关停优雅性小瑕疵，owner TTL/takeover 兜底。反向检索 WithTimeout/NewTicker/HTTP body 未发现新的泄漏路径。未运行测试。 |
| error-boundary | 2026-06-22 | 0/0/0/0 | 06-22 复核未发现新增确认问题。旧 P1 `requireVoiceRoomAnchor` 首行 `return nil` 在当前工作区已消除：helpers.go 会拒绝 userID<=0、调用 RoomApp.IsVoiceRoomAnchor，非主播返回 OnlyAnchorCanAccess；建房/主播收益/数据中心/开播游戏选项等入口仍复用该校验，`go test ./apps/app-service/internal/logic` 通过。旧人工确认 Stream 先标记 dedupe 再投递的崩溃窗口，本轮判定为 stream/dedupe 交付折中而非错误吞噬：state convergence 不跳过、dedupe backend 失败时宁投递不丢、缺 requestId 有错误日志和指标。抽查 claimsFromProto(nil) 的零值 claims：join/leave/管理信令、gift submit、coupon send/claim、anchor follow、room share 均在 logic 或 application 层 fail-closed。log 后继续项多为明确 best-effort；礼物 notify 标记失败、presence best-effort、gift 金额溢出已分别在 consistency/money 主题记录，不重复计数。 |
| resilience-overload | 2026-06-22 | 0/0/3/0 | 06-22 复核：旧 P2 中 public `/v1` body 上限已修，AuthMiddleware 使用 8MiB LimitReader；旧 P2 Ticker 根 ctx 问题已修，Ticker.runOnce 为每轮 Run 创建 deadline context 并记录 timeout/slow/panic 指标；旧 P3 外部 HTTP 响应无 LimitReader 已修，userauth/internalapi/RongCloud 响应读取走 internal/httpbody.ReadResponseBody(4MiB)。仍 open P2-1：room-state enrich / GetUserInfos 仍逐 user 串行调用 GetUserInfo，缓存 miss 时每人一次 HTTP（UserInfoAPI.Timeout=3s），冷缓存大房间下延迟随成员数线性增长，且发生在房态 mutation 后、ack/dispatch 构造前。新增 P2-2：`GET /ws` 是 public route，解析正数 uid + 非空 skey 后会先调 `UserAuth.AuthCheck`（3s timeout），`MaxConnsPerUser` 与 `MsgRateLimit` 都在其后才生效；缺少 IP/global handshake 频控时，伪造握手洪泛会把公网请求放大成上游 auth HTTP 压力。新增 P2-3：RongCloud public callback 在 gateway 验签前先 `ParseForm`（未套 MaxBytesReader 时标准库默认最多 10MB）、`json.Valid` content/extraContent 并转发 app-service RPC；无效签名大表单仍会消耗 gateway/RPC 资源。检查通过：公网 authed body/响应体/ticker deadline 三项旧风险已证伪；未发现新的 P0/P1。 |
| contract-alignment | 2026-06-22 | 0/0/0/0 | 06-22 复核：旧 P2 room/list 与 recommend 的 `createdAt` 秒/毫秒漂移已消除；旧 P3 `sl_anchor_profile` 快照列残留已消除。早先新增的 P2 `/v1/room/list hasMore` 合同问题已由当前工作区 Go 变更修复：repo 多取一条探测下一页，application 层透传 repo 返回的 `hasMore`。早先新增的 P3 `guild-live-room-design.md` 月表维度漂移已修复，文档明确 `_earning_monthly` 是每主播每月一行、公会字段为月内快照，公会维度月报应从 daily 聚合或另建汇总。早先新增的 P3 public signalling WS 配置漂移已修复，附录 E 拆分代码默认值与当前 gateway.yaml 示例值，并说明读写 buffer 是代码常量；已运行 `GOCACHE=/tmp/wa-broadcast-go-build scripts/gen/gen-docs.sh` 同步汇总和 docsassets。提示词运行正常，无回改。 |
| perf-capacity | 2026-06-22 | 0/0/3/1 | 06-22 复核：仍 open P2：room-state enrich / GetUserInfos 逐 user 串行调用 GetUserInfo，缓存 miss 每人一次 HTTP（UserInfoAPI.Timeout=3s），冷缓存大房间下房态 ack/dispatch 构造延迟线性增长；主播礼物墙 ListAnchorGiftWall 虽已由 active gift 维表驱动，但 sl_gift_settlement 缺 `(receiver_user_id,payment_status,gift_id)` 或汇总，主播历史 paid 明细多时仍会按礼物维度反复扫 receiver/status 历史段；新增 P2：/room/data/detail 的礼物明细按 room_id+sender_user_id+payment_status 聚合 gift_id，但 settlement 只有 `(room_id,payment_status,paid_at)`，忙房间里单送礼人下钻会先扫房间 paid 大段数据。P3/人工确认：粉丝列表 OFFSET 深分页仍在，但 pageSize capped=50 且 idx_anchor_status_time 支撑过滤排序，需确认是否有深翻场景。检查通过：粉丝列表不是无索引风险；room/date 聚合类 gift 查询可用 idx_room_payment_paid；本轮未发现 P0/P1。 |
| security-exposure | 2026-06-22 | 0/0/1/1 | 06-22 自由复核：旧 P0 日志泄漏主因已消失，`internal/userauth` 与 `internalapi` 现只记录 stime/nonce、状态码、耗时、字节数，不再打 `sign_key`/`sign_plaintext`/`request_body`/响应体明文。仍 open：P2 `web/signalling-debug` 默认 `clientSignSecret` 仍提交；P3 WS 仍用 query 传 `skey`（本服务日志已 mask，但浏览器历史、代理或上游 access log 风险仍在）。已处理：用户信息成功/缓存命中日志已移除 `name=%q`，保留 `user_id/status/duration/response_bytes/has_avatar` 等排障字段。原 06-16 记录：P0 为网关鉴权与 app-service 外呼日志泄漏可重放凭证；当时检查通过项包括 env 模板、错误返回屏蔽、RongCloud 回调跳过 signature/content/extraContent、WS 出站 payload 递归 redaction、生产 docs 环境开关、`go.mod` 无 replace。 |
| release-ops | 2026-06-22 | 0/0/0/3 | 06-22 复核：旧 P2 gift reconcile schema 错序上线已由 `runtime_schema.go` + app-service `-schema-check` + `preflight-services.sh` 覆盖，构造期和裸机启动前都会拦缺字段；旧 P3 compose 冷启动项已修，dev/prod compose 均给 app-service 配 healthcheck 且 gateway `depends_on.condition=service_healthy`。当前 open P3-1：`scripts/data/run-settlement.sh` 默认加载 `apps/app-service/etc/.env`，当前该文件存在，命令本体无 dry-run/yes/environment guard，会直接重跑收益结算与转化/ROI；因管线可重跑且是手工入口，按低门槛误操作风险记录。新增 P3-2：`scripts/dev/up.sh prod` 无确认启动 prod compose，并混入本机 `apps/*/etc/.env` 作为运行时 secret/env，可能误连真实依赖或暴露 8080。新增 P3-3：`scripts/services/stop-services.sh` 仅凭 pid 文件 kill/kill -9，未校验 pid 仍是当前 install dir 的 app-service/gateway，stale pid 被复用时可能误停同机进程。检查通过：配置 ValidateRuntime 两侧构造期 fail-fast，start/restart 均先 preflight，schema-smoke 仍导入一次性库校验表数。提示词运行正常，无回改。 |
| observability | 2026-06-22 | 0/0/0/1 | 06-22 复核：旧 R1/R2/R3 P3 均仍已修，WS send queue full 日志/指标带 conn/user/queue/queued/capacity，roomactor inbox timeout 带 room/node/phase/queued/capacity 且 panic 记录 stack，多节点 dispatch stream publish 逐 node 记录失败并 errors.Join；旧 trace 人工确认已在 `docs/ops/observability.md` 明确为阶段性决策。旧“gateway 指标端口/业务失败聚合”已补齐：gateway.yaml 显式 Prometheus，load-test runbook 记录 `GATEWAY_PROM_PORT`、PromQL 与 scrape 状态标注，gateway HTTP/WS 与 app-service ProcessSignal 均有 bounded result 指标。新增 P3：GiftReconcileJob 自动对账耗尽进入 `manual_reconcile` 时只打 `automatic attempts exhausted` 日志，`app_service_gift_reconcile_actions_total` 仍归为 `unchanged`，Prometheus 无法区分正常等待 retry 与已停止自动补偿的待人工对账订单。验证：`go test ./apps/app-service/internal/metrics ./apps/app-service/internal/job -run 'Test(.*GiftReconcile|.*Metrics)'` 通过。提示词运行正常，无回改。 |
| test-effectiveness | 2026-06-22 | 0/0/1/0 | 06-22 复核：旧 P1 master HEAD 单测红已关闭，`GOCACHE=/tmp/wa-broadcast-go-build go test -count=1 ./...` 当前通过；旧 P2 黄金结算 integration 未进 CI 已关闭，ci.yml 当前运行 `make anchor-earning-integration`，并额外运行 app-service workflow、room-session、race、schema-smoke 门禁。已修 P2-1：信令事件日志失败注入已补 `TestRoomJoinStillSucceedsAndDispatchesWhenAppendEventFails` 与 blacklist append-event 失败用例。已修 P2-2：信令后置副作用失败已补 `TestProcessRoomCloseReturnsAckWhenSummaryLoadFailsAfterMutation` 与 `TestProcessRoomLeaveReturnsAckWhenActiveRoomCleanupFailsAfterMutation`，覆盖 close summary 与 active_room/presence cleanup 失败后仍返回 ack/dispatch。已修 P2-3：房间礼物 paid 后 `ApplyGiftHeat` 保存失败已补 `TestProcessGiftSubmitKeepsSuccessWhenApplyGiftHeatFailsAfterPayment`。本轮新增热路径覆盖：`TestRevokedSnapshotAdminCannotKickMember`、`TestProcessSignalRejectsBlacklistedMemberBeforeRoomMutation` 与 `TestProcessGiftSubmitRejectsBlacklistedMemberBeforeCharge` 已覆盖 stale admin / stale blacklist 不再继续行动或扣款送礼。仍 open P2-4：房间管理测试只覆盖 DB 与 snapshot 同时成功，没有模拟 `GrantRoomAdmin`/`RevokeRoomAdmin`/`PersistAcceptedRoomAdminInvite`/`UpsertAnchorBlacklist` 已提交后 `SaveSnapshot`/`SetOwned` 失败。早先新增的 P2-5 `/v1/room/list hasMore` 测试固化错误合同已由当前工作区 Go 变更修复，新增 `TestListActiveRoomsForwardsHasMore` 并移除错误断言。提示词运行正常，无回改。 |
| debt-cleanup | 2026-06-22 | 0/0/0/0 | 06-22 复核未发现确认问题。旧 P1 `requireVoiceRoomAnchor` 工作区调试短路已消除，helpers.go 当前会拒绝 userID<=0、调用 RoomApp.IsVoiceRoomAnchor，go vet ./apps/app-service/internal/logic 通过，git status 未见 helpers.go 残留。U1000：`staticcheck -checks U1000 ./...` 在提权+/tmp module cache 下完成，仅命中两个 `_test.go` helper，按规则过滤后生产代码干净。工具缺口：`deadcode` 仍未安装，不能声称全程序 exported dead code 已扫完。旧 over/debt 残留复核：emptyRoomGiftCloseRetryQueue 已无命中，emptyroomcleanupjob 复用 roomGiftCloseRetryQueue；DeleteNodeStream 注释已说明未接入 stale cleanup、作为未来 multi-gateway stream 清理 seam 保留；旧失真注释无命中。生产 fake/mock/stub/testOnly 标记无命中。提示词回改：`debt-cleanup.md` deadcode main 入口从旧“当前 4 个”更新为当前 9 个 main 入口（含 bootstrap-anchor-guild-bindings 与 tools/*）。 |
| counterfactual-baseline | 2026-06-23 | 0/1/2/1 | 三轮广深审计发现 1 个 P3；随后按用户要求从严格反事实口径扩展为纯问题搜索，继续追加确认 P1/P2。候选池按三路合并：entry-map 的 job/双写/状态机/对账入口 + grep `reconcile/retry/fallback/pending/outbox/dirty/compensate/ensure/restore/flush`、状态字段、helper/repo、治理注释 + schema 中 queue/dead/delivery/event/log/request/presence/action/settle/reconcile 表与 claim/cursor/token/attempt/status 索引；第三轮继续扩到 claim/attempt/retry/next_retry_at/claim_token/last_*_at 和 best-effort/兜底/收敛注释。确认 P1：WS `room/block` 入口只在 router 校验 claims 非空，直接进入 `handleRoomBlock`/`addAnchorBlacklist`；后者先 `HasVoiceRoomAnchor`、`UpsertAnchorBlacklist`，再在 `input.RoomID>0` 时调用 `RevokeRoomAdmin(roomID, blockedUserID, claims.UserID)`，最后才进 `removeBlacklistedUserFromRoom` 的 actor `Mutate` 校验 `current.HostUserID == claims.UserID`。`RevokeRoomAdmin` SQL 条件只有 `room_id/user_id/status`，因此一个主播若对非自己房间发送 `room/block` 且目标是该房管理员，请求最终会 unauthorized，但管理员 DB 行已经被撤销。HTTP `/v1/anchor/blacklist/add` 因 `loadManagedActiveRoom` 前置校验不受此问题影响；问题集中在 WS `room/block` 旁路。确认 P2：当前最新脏 diff 已把 `room_text_mute` 的授权校验放回 `roomMutator.Mutate` 内，但 `UpsertRoomTextMute/RevokeRoomTextMute` 又移到 actor 成功返回之后；roomactor 在返回前已 `SetOwned` 持久化 snapshot，因此 DB 写失败会留下投影与事实源分裂：mute 方向房态/self 已显示禁言但融云回调只按 `GetActiveRoomTextMute` 放行，unmute 方向房态已显示解除但 MySQL active 行仍可能继续拦截。新增测试只证明手动重试可收敛，不能覆盖不重试窗口。新增 P2：`mic/invite` 发送路径先 `SavePendingInvite` 写 Redis，再进入 actor 执行 `ensureActiveAdminForManagerMutation`；actor 因非管理员或 stale admin 拒绝时只做一次忽略错误的 `DeletePendingInvite` 补偿。kick/block 清理 mic invite 时也只把 cleanup error 记日志并继续，若 outgoing invite 残留，pending invite 会通过 self/room pending 列表暴露；accept 路径只校验 invitee/room/座位，不复核 inviter 当前管理权限，可能让被邀请人从未授权或已失权邀请上麦。最小修复方向：`room/block` 的房内副作用必须先在 actor 当前态校验 host 后再撤 DB 管理员；`room_text_mute` 应在 `Mutate` 闭包内用 current 做授权并提交 MySQL 事实源，DB 失败时不保存 snapshot，避免要求客户端重试来补齐事实源；`mic/invite` 需把 pending invite 写入放到 actor 当前态授权通过之后，或在 accept 时复核 inviter 权限/授权 epoch，避免补偿失败留下可接受事实。确认 P3：私聊业务卡 MessageExpansion 状态更新的应用层错误返回机制被更短方案支配。`coupon.IMDispatcher.UpdatePrivateMessageStatusToClaimed` 与 `admininvite.IMDispatcher.UpdateInvitePrivateMessageStatus` 都声明返回 error，调用方也写了错误分支，但生产适配器只是调用 `im.Service.UpdatePrivateMessageExpansionStatus` 后固定 `return nil`；而该 IM service 方法本身无返回值，内部已对 `ListIMMsgRefsByBiz` / `SetPrivateMessageExpansion` 失败逐条日志。因此这些应用层错误分支在生产不可达，只剩测试缝和误导性合同。胜出候选：把状态更新接口改为无返回值并删除调用方死分支，依赖 IM service 既有日志；在 refs 查询失败、单条 expansion 失败、无 refs、成功更新等场景均不比当前生产行为更差，且代码/测试合同更短。反向候选“让 IM service 聚合返回错误”也可修，但不是更短最小解。其余已深挖不报：HTTP anchor blacklist add、房管 DB+snapshot、房管邀请 pending/status、礼物关房 retry/stale scanner、gift reconcile、room notice fanout/claim token、dispatch stream recovery/dedupe、presence cleanup/reconcile、display dispatch claim/reset/log、切房清理、建房幂等、revenue boost correction、roomshare attribution、official notice/replay lock、coupon claim action；这些均存在候选在 stale snapshot 越权、closed room pending 窗口、钱包 unknown、通知重复/丢失、subscriber gap、cleanup 丢失、并发 publish、切房残留、建房重试、boost correction 失败、进房延迟、replay 防护或分析日志缺失等格子变差，未形成支配/确认问题。提示词可执行，无回改。 |
| counterfactual-baseline | 2026-06-23 | 0/0/1/0 | 第四轮继续纯问题搜索，新增 P2：`room/join` 先经 actor 保存成员和 room state snapshot，随后 `applyActiveRoomSideEffects` 才写 Redis `ActiveRooms.SetUserActiveRoom`。该写入失败时请求返回错误，但已保存的成员关系不会回滚，也不会创建 presence cleanup；下一次加入其他房间时 `ensureRoomJoinAllowed` 只查 active_room 索引，索引缺失则放行，用户可被保存为多个房间成员。网关 cleanup 在多 reconnect room 且无 active_room match 时会跳过，presence cleanup/stale reconcile 也依赖 active_room 或 presence 记录，不能证明能清掉这个“无索引成员”。最小修复方向：join 的互斥索引应在 actor 保存前 fail-fast 写入/占位，或 actor 成功后索引写失败时同步执行可验证回滚/补偿并阻止后续 join 继续绕过。已继续排除/暂缓：gateway pending join 在 send queue full 时 `enqueueLocked` 会关闭 channel，`writePump` 关闭 websocket，`readPump` defer 通过 `RemoveClient` 清 pending/connRooms，未形成稳定残留；close/host leave 的 `UpdateRoomFromSnapshot` 虽是 best-effort，但 dirty marker 保留且 `FlushRoomStateJob` 每 5 秒补刷重试，暂不按丢状态报告；房管邀请 status 半成功不会越权生效，accept 侧复核当前 host/黑名单并在 actor 内持久化授权；`AcceptRoomAdminInviteAllowsInviteeOutsideRoom`、`room/kick` 不撤销 DB 房管属于产品语义待确认，文档只明确 kicked 用户可重新 join，未明确踢出是否降权，先不按硬 bug 计数；roomshare/private gift 的 IM ref/额外文本失败是展示或追踪 best-effort；job claim/retry 多数有 claim token/fence 或下一轮重试。 |
| counterfactual-baseline | 2026-06-23 | 0/0/1/0 | 第五轮继续纯问题搜索，新增 P2：公网 authed `POST /v1/gift/private/submit` 只把 claims、`giftId/count/toUserId` 转给 app-service `SubmitPrivateGiftAction`；`application/gift.Service.SubmitPrivate` 校验 sender/to/gift/count 后直接 `SubmitAction`、钱包扣款、再由 `wire_gift.SendPrivateGiftMessage` 以服务端 `NewPrivateCustomMessage(... ObjectNameCustomMessageGiftSend).Persisted(true)` 发送私聊礼物消息。该路径没有查询 `IsAnchorBlacklisted`，而公开合同写明 `room/block` 后“不能给该主播发送私聊消息”；普通 RongCloud private text callback 的黑名单检查只处理用户私聊回调，不会保护这个服务端主动发送的礼物消息。结果是被主播拉黑的用户仍可对主播发起私聊礼物扣款和通知，至少绕过黑名单私聊合同，且扣款发生在任何 IM 失败处理之前。最小修复方向：私聊礼物 submit 在扣款前判断接收方是否主播；若是主播，复用 `IsAnchorBlacklisted(anchor=toUserID, blocked=senderUserID)` fail-closed，必要时同时复核主播是否已发起私聊。已排除：房间礼物 `gift/submit` 自己在扣款前调用 `ensureRoomSignalActorNotBlacklisted`，不依赖 router 统一 precheck。 |
| counterfactual-baseline | 2026-06-23 | 0/0/1/0 | 第六轮继续纯问题搜索，新增 P2：gateway dispatch recovery 的 `reconcileLocalRoom` 用本地房间 `clients[0]` 作为代表 claims 调 `GetRoomStateSync` 拉权威房态；但 `Hub.RoomClients` 来自 map 遍历，代表选择不稳定。若 consumer gap/漏派发后本地仍残留一个已被主播拉黑的连接，且该连接恰好成为代表，app-service `BuildRoomStateSyncEnvelope` 会因 `ensureUserNotAnchorBlacklisted` 返回 `AnchorBlacklistRoomEntryDenied`，gateway 只记录 `reconcile room state fetch failed` 并 `return`，不会像 resume 的 `filterResumableRooms` 那样移除这个 blacklisted room，也不会换一个仍合法的本地成员继续拉房态。结果是 recovery 本应清理的被拉黑/非成员连接可能继续留在本地 hub 和 conn subscription 中，其他合法成员也收不到这轮 full sync。最小修复方向：reconcile 时不要用任意本地 stale client 作为唯一授权代表；遇到 `AnchorBlacklistRoomEntryDenied` 应先移除该用户/连接并尝试其他本地成员，或改用房间级/服务端授权的内部 sync 入口，确保 recovery 对单个 stale client fail-closed 而不是整房间 fail-open。已排除：WS resume 的 `filterResumableRooms` 已专门处理黑名单错误并移除 invalid resumable room；该缺口只在 dispatch recovery reconcile 路径。 |
| counterfactual-baseline | 2026-06-23 | 0/0/2/0 | 第七轮继续纯问题搜索（校准：前几轮对 display/presence 类候选按“反事实支配”未报，本轮按运行时永久丢补偿风险重新确认）。新增 P2：`DisplayDispatchJob` 的 `ClaimDisplayDispatchForPublish` 先把 `sl_display_dispatch.last_dispatched_at` 置非空，`ListActiveDisplayDispatches` 又只捞 `last_dispatched_at IS NULL`；发布失败时仅 best-effort 调 `ResetDisplayDispatchPublishClaim`，reset 失败只打日志，随后追加 failed event log 并返回 publish error。若一次 gateway publish 失败叠加一次 DB reset 失败，运营展示实际未发布，但调度记录已被永久排除在后续批次外；测试只覆盖 reset 被调用，未覆盖 reset 失败后的不可重试状态。最小修复方向：claim 应有独立 `publishing/claim_token/claim_expires_at` 或 retry 状态，不应复用最终 `last_dispatched_at` 作为 claim；publish 失败且 reset 失败时必须保留可重试事实或进入显式 dead/manual 队列。新增 P2：后台房间 cleanup/reconcile 任务在删除重试候选/清索引后才发布补偿 dispatch，发布失败会永久丢“用户离开/房间关闭”通知：`PresenceCleanupJob` 对 leave 先 `ClearUserActiveRoomIfMatch` 并 `RemovePresenceCleanup`，对 close 也逐用户清 active_room/cleanup，然后才 `publisher.Publish`；`StalePresenceReconcileJob` 同样先 end presence、清 active_room/cleanup 或关闭房间副作用，再 publish；`EmptyRoomCleanupJob` 在 `AutoCloseEmptyRoom` 已关闭房间后 publish 失败只日志。由于这些是后台补偿任务，不存在前台请求 ACK 携带 dispatch，也没有 outbox/重放队列；若 Redis stream/pubsub 发布失败，在线其他成员只能等下一次主动 sync/resume 才可能纠正，补偿通知本身不会重试。最小修复方向：cleanup 类 dispatch 需要 durable outbox/重放，或至少在 publish 成功后再清理候选；publish 失败时保留可重试记录并让 mutation 幂等地避免重复副作用。已排除：`RoomNoticeSendJob` 在外部 `SystemSend` 成功后 `MarkSent` 失败会被 claim TTL 置 failed 而非 pending，当前不会稳定重复发送，只会造成本地失败态/审计不准；普通 WS 信令 dispatch 多为返回给 gateway 后本地发送/跨节点发送 best-effort，属于系统投递语义残余，不并入本轮后台补偿丢任务问题。 |
| counterfactual-baseline | 2026-06-23 | 0/0/1/0 | 第八轮继续纯问题搜索，新增 P2：融云聊天室文字回调只校验聊天室对应房间 active 和房间文字禁言，未校验发送人仍是房内成员，也未复用主播黑名单判定。`checkPrivateTextMessage` 对私聊主播会查 `IsAnchorBlacklisted`，但聊天室分支在 `GetVoiceRoomByRongChatroomID` 和 `chatroomTextMute` 后直接 `applyChatroomCommentHeatBestEffort` 并 `pass=1`；其中 `isCommentSenderInRoom` 仅用于决定是否加评论热度，返回 false 或检查失败只是不加热度/打日志，不会拦截消息。公开合同要求 `room/block` 立即踢出当前直播间，`room/enter`/`room/join`/resume 会因黑名单拒绝，且普通房内信令与房间送礼已在扣款/变更前做黑名单预检；但 RongCloud 公聊回调没有同等服务端兜底。若 `PushAnchorBlacklisted`/`PushRoomKicked` 驱逐 dispatch 丢失、客户端/融云侧仍保持聊天室发送能力，或用户已 leave/kick 但仍向旧 chatroom 发 `RC:TxtMsg`，服务端回调会放行，形成“已非房内成员/已被主播拉黑仍能公聊”的合同缺口。最小修复方向：聊天室文字回调应在 pass 前拉权威房态，要求 active + `snapshot.HasMember(senderUserID)`，并按 effective host 复用 `IsAnchorBlacklisted` fail-closed；被踢/离房可返回普通 reject，被拉黑可复用黑名单 extra 或新增公聊黑名单 extra。已排除：`ensure_chatroom` 与 `room/enter` 已有 snapshot 黑名单检查，缺口只在消息回调的发送时二次校验；公聊禁言 extra 文档只覆盖 text mute，不影响本项成员/黑名单兜底结论。 |
| counterfactual-baseline | 2026-06-23 | 0/0/1/0 | 第九轮继续纯问题搜索，新增 P2：房间开播通知 delivery 发送到融云时，一次 transient 错误会把整批接收人永久置为失败。fanout 创建 `sl_room_notice_delivery` 有唯一键和游标推进，发现阶段也有 `AnchorLiveNoticeFanoutMaxAttempts=5`；但真正发送阶段 `RoomNoticeSendJob.sendBatch` 调 `SystemSend` 后，只要返回 error 就立即 `MarkRoomNoticeDeliveriesFailed`，状态从 sending 变为终止/失败，后续 `ClaimRoomNoticeDeliveries` 只捞 pending，不会再重试。RongCloud client 把 HTTP/network error、非 2xx、API code != 200、messageUID 缺失/不完整都统一返回 error，job 没有区分永久业务错误和 5xx/timeout/响应校验不确定态；测试 `TestRoomNoticeSendJobMarksFailuresTerminal` 也固定了失败即终态。结果是一次外部抖动可永久丢最多 100 个粉丝开播通知，delivery 表只留下 failed 行和 last_error，不进入重试/死信/人工补发队列。最小修复方向：delivery 层增加 send_attempt_count/next_retry_at 或独立 outbox/dead 状态；transient/unknown send 错误回 pending 并退避重试，只有明确永久参数错误或超过上限才终止；对 messageUID 不完整这种“可能部分成功”的情况应记录 per-target 不确定态或补查/幂等发送策略，而不是整批永久失败。 |
| counterfactual-baseline | 2026-06-23 | 0/0/1/0 | 第十轮继续纯问题搜索，新增 P2：WS `room_admin/invite` 成功持久化 pending 邀请后，私聊邀请卡片发送是 best-effort；一次 IM 发送失败会留下没有可操作卡片/ticket 的 pending 邀请，并阻塞主播重邀 30 分钟。HTTP `/v1/room/admin/invite/send` 路径在 `AdminInviteApp.Send` 中 `CreateRoomAdminInvite` 后同步 `SendPrivateMessage`，失败会尝试 `RevokePendingRoomAdminInvite` 并返回错误；但 WS 信令路径 `handleRoomAdminInviteSend` 只创建 pending invite、保存房态版本、返回 ack/dispatch，然后 `ProcessSignalLogic.sendRoomAdminInvitePrivateMessageBestEffort` 异步尝试私聊，失败仅日志。接受 HTTP 接口 `AcceptRoomAdminInviteLogic.Accept` 必须从私聊卡片拿 `ticket` 并 `ParseTicketForInvitee`；公开 IM 文档也写明 `ticket` 为空展示不可操作卡片，而 room state patch 的 `RoomAdminInvitePatchPayload` 只有 invite/status/user/role/time 字段，不带 ticket。测试 `TestProcessSignalRoomAdminInviteKeepsPendingInviteWhenPrivateMessageFails` 明确固定了 IM 失败后 ack/dispatch 仍成功且没有 repo 补偿；`HasPendingRoomAdminInvite` 又会让同一房间同一 invitee 的后续发送返回 AlreadyPending。结果是融云瞬时失败可让被邀请人拿不到可接受凭证，主播重试被 pending 占位挡住，直到过期或手工取消。最小修复方向：WS invite 应与 HTTP send 对齐，把 ticket/outbox 生成纳入强一致链路；IM 失败时要 revoke pending 或进入可重试发送队列，并确保客户端可通过权威查询重新获取 ticket/卡片，而不是只靠一次 best-effort 私聊。 |
| counterfactual-baseline | 2026-06-23 | 0/0/1/0 | 第十一轮继续纯问题搜索，新增 P2：主播 revenue boost correction 先修正礼物/游戏明细，再重跑 daily/hourly/monthly 等下游汇总；若下游任一步失败，后续定时重试会丢失 affected dates，汇总可能永久停在旧金额。`AnchorRevenueBoostImportJob` 已修过“revision 已存在也继续调用 correction”，但 `CorrectAnchorRevenueBoostGiftSettlements`/`CorrectAnchorRevenueBoostGameLedgers` 的日期列表是在 UPDATE 前按 `receiver_ratio < b.gift_ratio_bps`、`income_amount < FLOOR(...)` 或 `ratio_bps_snapshot < b.game_ratio_bps` 查出来；UPDATE 成功后这些条件不再成立。随后 `anchorRevenueBoostCorrection.rerunAffectedDates` 逐日期执行 `rerunDaily`、`rerunHourlyPayout`、第二次 `rerunDaily`、公会失效、公会佣金、薪资周期、月汇总，任一步返回错误都会中断并让本轮 job 失败；但下一轮 correction SQL 已看不到已修正明细的日期，`affectedDates` 为空，无法补跑失败的下游汇总。测试只覆盖 happy path rerun 顺序和 existing revision 继续调用 correction，没有覆盖“明细已修正后 rerun 失败再重试”的恢复性。结果是 boost 明细收入已经提高，但 `sl_anchor_earning_daily/monthly`、小时薪资、公会汇总等派生表可能不再重算，报表/结算继续读旧汇总。最小修复方向：把 correction 做成带状态的 outbox/repair task，先记录 affected dates，只有所有下游重算成功后才标记完成；或把明细修正和日期任务登记放同一事务，重试依据任务表而非“明细仍低于 boost”这个会被 UPDATE 消除的派生条件。 |
| counterfactual-baseline | 2026-06-23 | 0/0/1/0 | 第十二轮继续纯问题搜索，新增 P2：活跃 WS `coupon/claim` 在外部领券成功后，本地优惠券领取 action log 只是 best-effort；写入失败会永久少计转化日报的领取人数/领取使用率。`processCouponClaim` 调 `CouponApp.Claim` 成功后直接返回 `status=claimed` ack；`coupon.Service.Claim` 先调用外部 `ClaimCoupon`，再更新私聊卡片状态，最后 `writeCouponClaimActionsBestEffort` 写 `AnchorActionTypeCouponClaim` 和 issue backfill，`GetAnchorActionBySourceEvent` 缺失或任一 `UpsertAnchorActionLog` 失败都只打日志不返回错误，也没有 outbox/补扫 job。与此同时 `SumAnchorConversionActionsByDate` 直接从 `sl_anchor_action_log` 按 `AnchorActionTypeCouponClaim` 统计 `coupon_claimed_user_count`，`AnchorConversionDailyJob` 用它计算 `CouponClaimedUserCount` 和 `CouponUseRateBps`；公开信令文档也把 `coupon/claim` 作为领取成功状态。结果是外部券系统已经完成领取、客户端也收到成功，但 IM/DB 瞬时失败导致本地分析事实永久缺失，后续转化率/ROI 口径偏低；未来首充券 MQ 虽未接入生产消费，也会放大这类缺失。最小修复方向：claim action/backfill 应成为领券成功后的可靠本地事实，至少写入失败进入 retry/outbox；若要保持业务 ack 不阻断，也需要持久化待补记录并从外部 logId/userCouponID 可重放补齐，而不是只日志。 |
| counterfactual-baseline | 2026-06-23 | 0/0/1/0 | 第十三轮继续纯问题搜索，新增 P2：融云私聊回调的主播发起私聊 action log 也是异步 best-effort，写失败会永久少计私聊转化/ROI。`checkPrivateTextMessage` 在发送人是主播时，会先用 `InsertPrivateChatInitiationIfAbsent` 强写私聊发起事实，失败会拒绝/返回错误；但随后 `recordPrivateChatActionBestEffort` 用 `threading.GoSafe` 异步调用 `writePrivateChatAction`，`resolvePrivateChatRoom` 失败还会降为 unknown，真正 `UpsertAnchorActionLog` 失败只日志。`SumAnchorConversionActionsByDate` 直接用 `AnchorActionTypePrivateChat` 统计 `private_chat_user_count`，`AnchorConversionDailyJob` 用它计算 `PrivateChatRateBps` 和发券率分母；`SumAnchorROIActionsByDate` 也用同一 action type 统计 `chat_user_count`。结果是用户和主播私聊已被融云放行且本地 initiation 已存在，但分析事实没有可靠补偿来源；一次 DB 抖动或 goroutine 超时会让当天转化漏掉该私聊用户，并连带影响后续发券率/ROI。最小修复方向：私聊 action log 应与 `private_chat_initiation` 同步/同事务或进入 durable outbox；若为了不阻塞回调，至少用 messageUID/source_event_id 写待补任务并由后台重试，避免回调成功后只靠日志保留丢失事实。 |
| counterfactual-baseline | 2026-06-23 | 0/0/1/0 | 第十四轮继续纯问题搜索，新增 P2：HTTP/RPC 房间管理入口在事实源已变更后，跨 gateway 派发失败只日志并仍返回成功，在线客户端可能保留旧权限/旧房态。`CloseRoomLogic.closeLocal` 先 `MutableRoomApp.ProcessSignal(room/close)` 关闭房间，再 `GatewayDispatcher.Publish`，失败仅 `publish room close dispatches failed`；`AddAnchorBlacklistLogic.addLocal` 先 `MutableRoomApp.AddAnchorBlacklist` 拉黑/踢人并清 active_room/presence cleanup，再 publish 黑名单/房态 dispatch，失败只日志；`RemoveRoomAdminLogic.removeLocal` 先 `MutableRoomApp.RemoveRoomAdmin` 撤销 DB/房态房管，再 publish revoke dispatch，失败只日志；`AutoCloseEmptyRoomLogic.AutoCloseOnOwner` 也在 `AutoCloseEmptyRoom` 后 publish 失败只日志；同族低影响入口还包括 `UpdateRoomLogic.updateLocal` 更新房间资料后 publish 失败只日志。前台请求/RPC 返回成功时没有 outbox、retry 或返回 dispatch 给调用方兜底，和 WS `ProcessSignal` 的 ack+dispatch 同响应语义不同。结果是被拉黑用户、被撤房管用户或房间成员如果正连在其他 gateway 节点，可能继续看到旧房态/旧角色，直到下一次主动 `GetRoomStateSync`、resume、重连或其它派发触发；对 close/blacklist/revoke 这类强一致 UX/权限事件尤其明显。最小修复方向：管理入口的 dispatch publish 需要 durable outbox/stream retry，或在 publish 失败时返回可感知错误/重试状态；至少对 close/blacklist/revoke 这类权限收敛事件，应保证失败后有后台 reconciliation 继续推送/踢连接，而不是仅日志。 |
| counterfactual-baseline | 2026-06-23 | 0/0/1/0 | 第十五轮继续纯问题搜索，新增 P2：房态快照落 MySQL 时，`sl_voice_room` 主表更新成功后成员投影同步失败会被吞掉，随后 Redis dirty 标记仍被清理，导致 `/get_room` 成员列表和房间聚合计数可能长期停在旧状态。`Repository.UpdateRoomFromSnapshot` 先事务更新 `sl_voice_room` 版本/状态/online_count，提交后调用 `syncRoomMembersFromSnapshotBestEffort`；成员表 SELECT/INSERT/UPDATE/offline/guest/`refreshRoomMemberAggregates` 任一步失败只记录 `sync room members from snapshot failed` 并返回 `(applied=true, err=nil)`。`MutableService.updateRoomFromSnapshotBestEffort` 与 `FlushRoomStateJob` 看到成功后都会调用 `RemoveDirtyIfVersion` 删除该版本脏标；而 `Repository.GetRoom` 会从 `sl_voice_room_member WHERE online_status=1` 加载成员，`GetRoomLogic.Get` 再把这些成员返回给 RPC 客户端，`refreshRoomMemberAggregates` 也反写主表 `online_count/current_guest_count/total_guest_count`。测试 `TestUpdateRoomFromSnapshotIgnoresMemberSyncFailure` 明确固化了成员同步失败仍返回成功。结果是权威 Redis snapshot 已推进并被认为落库完成，但 DB 成员投影/聚合可能缺新成员、保留已离房成员或错误 guest 计数，后续没有同版本补偿机会，直到下一次房态版本变化碰巧重刷。最小修复方向：成员投影与主表快照应同属可重试一致性边界；失败时不要清 dirty，或写 durable repair task/outbox 并按 room_id+version 重放，成功刷新成员和聚合后再确认该版本落库完成。 |
| counterfactual-baseline | 2026-06-23 | 0/0/1/0 | 第十六轮继续纯问题搜索，新增 P2：gateway `room/join` barrier 在全量 `room/state_sync` 拉取失败或补洞失败时会把已成功进房的连接降级成 ack-only/active-without-replay，违反客户端“join Ack + state_sync 才进入房间态”的协议合同。`handleRoomJoinResponse` 先 `startPendingJoinSubscriptions` 把连接加入 hub pending 订阅并注册 Redis conn subscription；随后 `fetchRoomStateSync` 失败时只日志 `degrading to ack-only join`，调用 `finishJoinWithoutSync` 激活 pending join 并只 enqueue Ack。pending buffer overflow 后刷新 sync 失败同样 ack-only；buffered patch 出现 version gap 后刷新 sync 失败则 `ActivatePendingJoin` 后直接返回 nil，连 Ack/补洞 patch 都不补；activation 后 tail overflow/needResync 再次拉 sync 失败也只返回 nil。辅助函数 `finishJoinWithoutSyncLocked` 明确只激活 pending join 和发 Ack，不发送 full snapshot。公开文档要求 `room/join` “触发一份完整 room/state_sync”，客户端对接顺序是 Ack 后等待同连接 `room/state_sync` 初始化本地房态，前端状态机也是 `ROOM_JOINING -> ROOM_ACTIVE: 收到 room/join Ack + room/state_sync`；state_patch 附录要求先以完整 sync 初始化，再按连续 version 应用 patch。现有集成测试覆盖 Ack+Sync happy path 和 pending patch replay，但没有覆盖 sync 拉取失败的降级分支。结果是客户端可能认为 join 成功且服务端已订阅，随后收到的都是无基线的增量 patch；本地 UI/麦位/成员/权限状态可能缺失或因版本断档无法应用，用户只能靠手动重连或再次 same-room join 偶然恢复。最小修复方向：join 后拿不到 full sync 应保持 pending 并返回错误/要求客户端重试，或关闭/回滚本地订阅；对 overflow/version gap 场景，必须拿到新 full sync 后才能激活连接，不能把“无基线 active”作为成功路径。 |
| counterfactual-baseline | 2026-06-23 | 0/0/0/1 | 第十七轮继续纯问题搜索，新增 P3：`room/enter` 带分享 token 时，分享访问归因只进本进程内存队列，队列满、服务关闭或 worker/DB 超时都会永久丢失 `sl_room_share_visit` 事实。`EnterRoomLogic.Enter` 在已经构造 RTC/IM 进房响应后调用 `submitRoomShareAttribution`；该函数只把 token/room/visitor 投递给 `RoomShareAttributor.Submit`，失败仅记录 `room share attribution dropped: queue_full_or_closed`，不影响进房响应。`Attributor` 使用固定 256 容量 channel、8 个 worker 和 500ms timeout；队列满直接返回 false，worker 中 `ParseTicket`、`GetRoomShareByID`、`LookupUserNewness`、`UpsertRoomShareVisit` 任一步失败也只日志，任务不重试、不落库、不回压。`sl_room_share_visit` 是独立 schema 表，按 `(share_id, visitor_user_id)` 记录首次访问和 visit_count；文档也说明语聊房侧只提供 `sl_room_share` / `sl_room_share_visit` 这类分享访问事实，不能替代注册归因。结果是高峰进房或短暂 DB 慢查询时，用户已经成功通过分享进房，但访问事实和计数永久少记；由于当前未见它直接进入核心结算，只按分享分析/外部任务参考事实的低优先级 P3 记录。最小修复方向：把分享归因改为 durable outbox/队列表，至少按 shareToken+visitor 持久化待处理任务并退避重试；若仍内存队列，需暴露 dropped/failed 指标并提供可重放来源。 |
| counterfactual-baseline | 2026-06-23 | 0/0/0/1 | 第十八轮继续纯问题搜索，新增 P3：主播薪资周期升级时，profile 等级已更新后若等级变更日志写入失败，后续重跑不会补 `sl_anchor_level_change_log`，审计轨迹永久缺口。`AnchorSalaryCycleJob.RunForDate` 对每个候选先 `UpsertAnchorSalaryCycle`，再在 `candidate.CurrentLevelID != settledLevel.ID` 时调用 `UpdateAnchorProfileLevel` 更新 `sl_anchor_profile.level_id`，只有 `updated=true` 后才 `AppendAnchorLevelChangeLog`；日志写失败会让 job 返回 error。候选 SQL `ListAnchorSalaryCycleCandidates` 每轮直接从 `sl_anchor_profile` 读取 `level_id AS current_level_id`，不排除已结算周期；因此下一轮虽然仍会重算同一 cycle，但由于 profile 已是 `settledLevel.ID`，`candidate.CurrentLevelID != settledLevel.ID` 分支不再进入，无法补写上一次失败的 level change log。`UpsertAnchorSalaryCycle` 的 conflict 更新字段也不包含 `from_level_id`，cycle 表仍保留旧 from_level_id，但变更日志缺失。结果是业务等级已经生效、薪资周期也能重跑收敛，但审计/排障看不到这次系统升级原因和指标值。最小修复方向：等级更新和变更日志应同事务提交，或用唯一键 `(anchor_user_id, from_level_id, to_level_id, source_type, created_at/cycle)` 做幂等日志补偿；重跑时可根据 cycle 的 from/settled level 与缺失日志补写，而不是只看当前 profile level。 |
| counterfactual-baseline | 2026-06-23 | 0/0/0/2 | 第十九轮继续纯问题搜索，新增 P3：礼物 IM 通知成功后，本地 `notify_status` 标记失败仍只日志且无 pending/partial 补扫，历史 review 已记录但当前代码仍可复现。房间礼物 `processGiftSubmit` 在扣款成功、构造 ack/dispatch 后异步 `notifyGiftSentBestEffort`；融云 `SendChatRoomCustomMessage` 成功后逐 target 调 `GiftService.MarkActionTargetNotified`，失败仅 `mark gift record notified failed`。私聊礼物 `application/gift.Service.notifyPrivateGift` 同样先 `SendPrivateGiftMessage`，后 `MarkActionTargetNotified`；标记失败只打 `mark private gift record notified failed after rongcloud send succeeded` 并返回 nil。仓库 `MarkGiftActionTargetNotified` 会更新 `sl_gift_settlement.notify_status/notified_at` 并回写 `sl_gift_action.notify_status`，但当前仓库只见 charging 支付对账与 stale pending 关闭，未见扫描 `paid + notify_status in (pending, partial)` 的通知补偿 job；`docs/planning/project-review-findings-2026-06-17.md` 也把它列为 open 的礼物通知补偿缺口。结果是 IM 实际已发、客户端/融云可见，但本地状态长期 pending/partial，后续房间数据、排障和任何基于 notify_status 的补偿判断都会偏离；私聊成功响应还把 action 级 `GiftActionResult.NotifyStatus` 赋成 target 级 `VoiceRoomGiftNotifyStatusNotified=2`，与 action 级 `GiftActionNotifyStatusNotified=3` 枚举口径不一致。最小修复方向：通知发送应有 durable target outbox 或 paid+pending 补扫；标记失败不能只日志，至少要保留可重试事实；同时拆清 action/target 两套 notify_status 枚举，避免 API 返回混用。新增 P3：公网 `/anchor/follow` 与 `/anchor/unfollow` 的写限流在 Redis/limiter 错误时 fail-open，保护面会在依赖故障时消失。gateway 公开路由直接转 RPC `FollowAnchor/UnfollowAnchor`；app-service `anchorfollow.Service.Follow/Unfollow` 在写 `sl_anchor_follow` 前调用 `allowFollowRate`，生产 limiter 是 Redis go-zero limiter，固定 `10s/20`；但 `allowFollowRate` 遇到 `TakeCtx` 错误只记录 `anchor_follow,error_allowed` 并返回 nil，测试 `TestFollowRateLimiterErrorDoesNotBlock` 明确固化 Redis unavailable 时关注成功。结果是 Redis 抖动或限流器异常时，用户可以无限关注/取关刷写关注关系、粉丝计数和开播通知 fanout 基础表；这不是数据一致性 bug，但属于抗滥用/背压边界 fail-open。最小修复方向：关注/取关写接口对 limiter error 应 fail-closed 或退到本地短窗降级配额，并把 `error_allowed` 改为告警而非默认通过。已排除/降级：建房先创建融云聊天室再落本地房间会在本地失败时留下外部孤儿 chatroom，但未见影响用户可见事实；关注列表资料加载失败只降级头像昵称，presence/live room 加载失败会返回错误，非本轮问题。 |
| counterfactual-baseline | 2026-06-23 | 0/0/1/0 | 第二十轮继续纯问题搜索，新增 P2：成功 `room/join` 的成员在房 presence 创建失败后没有补建路径，转化/ROI/归因会永久漏进入房事实。`processGiftSubmit` 等房内路径之外，普通进房在 `ProcessSignalLogic.applyActiveRoomSideEffects` 里先 `ActiveRooms.SetUserActiveRoom`，再在 `result.RoomJoinApplied` 时调用 `createMemberPresenceBestEffort`；`CreateMemberPresence` 插入 `sl_voice_room_member_presence` 失败只打 `create member presence failed`，不影响 join ack/dispatch，也不保留 outbox。后续连接 TTL 会写 Redis presence heartbeat，`PresenceHeartbeatFlushJob` 调 `UpdateMemberPresenceHeartbeatByUser` 后只要没有 SQL error 就删除 heartbeat；而该 UPDATE 条件是 `room_id/user_id/left_at IS NULL`，如果初始 presence 行根本没创建，RowsAffected=0 不会报错，heartbeat 也被当作已 flush 删除，不能补建。关房链路只回填缺失主播 presence 以保护薪资结算，不能补普通用户进入房记录。`AnchorConversionDailyJob` 的 L1 `entered_user_count` 直接从 `sl_voice_room_member_presence` 按 `joined_at` 统计非主播用户，`ResolveAnchorRoomByPresence` 也用 presence 给私聊 action 归因房间，游戏流水归因辅助查询同样依赖该表。结果是用户已成功进房并可能完成私聊/游戏/消费，但一次 DB 插入抖动会让 entered user 分母、私聊房间归因和相关转化/ROI长期偏低或落 unknown。最小修复方向：join 成功后的 presence 创建应是可靠事实边界；要么在 ack 前硬失败并回滚/阻断 join，要么把 room_id/user_id/joined_at 写入 durable repair task；heartbeat flush 遇到 0 rows 应补建或保留 heartbeat/发告警，不能静默删除。 |
| mode-change-review | 2026-06-22 | -（diff 变更自查） | 06-22 先审计文档/ledger diff，修正 `Security / Authz Addendum` Current Result 误写 `0/0/0/3`（实际 1 个 P3）。随后审查 room/list hasMore Go diff：repo 多查一条、application 透传 `hasMore`、测试覆盖 full page with/without next page，未发现新问题，提权测试通过。继续审查 gift amount/heat overflow diff：`SubmitAction` 补 count/targets 上限和 checked 金额/收益/热度计算；审查中发现累计房态热度仍可能 wrap，已补 `AddVoiceRoomHeatScore` 饱和加法和 `TestApplyGiftHeatCapsAccumulatedHeatAtInt64Max`；未发现新问题。继续审查 `AppendSignalEvent` best-effort diff：event log 明确为审计日志，所有调用点改无返回值并补 join/blacklist 失败注入测试；未发现新问题，关闭 consistency P1-1。继续审查 gift paid-after heat failure diff：paid 后 `ApplyGiftHeat` 失败改为日志降级并继续构造礼物主事件/收入 patch/IM 通知，新增 `TestProcessGiftSubmitKeepsSuccessWhenApplyGiftHeatFailsAfterPayment`；未发现新问题，关闭 money P1-1，剩余 heat 补扫作为 P3。继续审查 coupon issue action diff：发券本地 issue action 写入失败时不再成功回包/发 IM，首存券 MQ 缺 issue 时返回错误重试；未发现该 diff 引入新问题，但外部 SendCoupon 仍无 requestId 幂等/outbox/reversal，残余 P1 保留。继续审查信令后置副作用 diff：close summary、active_room/presence cleanup 失败改为日志降级，不再阻断已落地信令 ack/dispatch；RTC grant refresh 改为 mutation 时携带 channel/发布权限，构造 dispatch 时不再后置回读 RoomState，BuildRoomToken 失败仍是硬业务边界；清理 helper 改为聚合错误并继续批处理；新增 close summary 与 active_room/presence cleanup 失败注入测试；未发现新问题，关闭 consistency P1-2，剩余清理类统一指标/补偿队列作为 P3。继续审查房间管理 stale admin/blacklist 热路径防护 diff：manager 操作前重查 active admin，普通房间信令和 `gift/submit` 扣款前重查主播黑名单，新增 stale admin kick、blacklisted mic/take mutation-before-fail 与 blacklisted gift before charge 测试；未发现新问题，旧房间管理 P1 降为 P2 投影/补偿风险。验证：提权跑 gift overflow targeted tests 通过；`go test ./apps/app-service/internal/application/gift` 通过；`GOCACHE=/tmp/wa-broadcast-go-build go test ./apps/app-service/internal/application/room ./apps/app-service/internal/logic` 通过；提权跑 `go test ./apps/app-service/internal/logic -run 'TestProcessGiftSubmit'` 通过；`go test ./apps/app-service/internal/application/coupon` 通过；`go test ./apps/app-service/internal/job -run 'TestFirstPayCouponIncomeConsumer|TestAnchorTaskRewardConsumer|TestVoiceRoomGameFlowConsumer'` 通过；提权跑 `GOCACHE=/tmp/wa-broadcast-go-build go test ./apps/app-service/internal/logic` 通过。 |
| mode-over-implementation | 2026-06-15 | -（不按 P 分级；复议后：可留为主 + 注释失真/死代码各 1） | 全项目通扫，聚焦 Stage 2 Stream 派发链路。可留：durable 单节点 stream+去重+pending 恢复+replay-age 可靠性内核（非过度）；Shadow 双写为迁移期临时件，cutover 后随 Pub/Sub 移除。**复议更正（撤回初版"应简"）**：NodeRegistry/heartbeat/ActiveNodes 不算过度——除扩容预留外，单实例即有价值：ActiveNodes 空时返回 ErrNoActiveDispatchNodes，把"静态 NodeID 配错→写到无人消费 stream→静默丢消息"换成显式失败，且 NodeID 变更自愈。**事实更正**：DeleteNodeStream 仅测试引用、无生产调用，CleanupStaleNodes 只 SREM 不删流——初版"DeleteNodeStream-on-stale 在跑"描述有误。真实残留收敛为 debt-cleanup 级：注释失真（node_registry.go:74 "callers can release per-node stream state"、dispatch_stream.go:206 "used when a node is cleaned up as stale"）+ DeleteNodeStream 孤立死代码（留作未来 seam 则应接线，否则删）。binding log 上轮"生产未写入"已被本次资金结算修复链路接线，非过度。**第二轮（同日，锚定资金结算修复链路 f68ca82f）：未发现明确过度实现**——时点归属（effective_from/to + binding LEFT JOIN）是修上轮 P1 漂移的直接正解、被结算重度使用；可留 2：binding log 审计字段（source_type/operator/reason write-only，但 money 管理动作的低可逆审计捕获）、gift reconcile 指数退避重试（成比例的 money 路径硬化，常量非配置）；可选微优化 1：lockOpenAnchorGuildBindingLog 多 SELECT 了 from_guild_id 等未用列。与第一轮 Stream 链路相反，结算链路是"瘦"的。**第三轮（逐包穷尽，全项目 ~45k loc/~25 包）**：深读 application/room、job、Stream、结算链路；其余包跑"投机标记/测试缝函数字段/机制关键词/防御关键词/领域层接口"电池 + 接口抽样。结果——全项目唯一新增发现是 1 处抽象小尾巴：emptyRoomGiftCloseRetryQueue(emptyroomcleanupjob.go:56) 与 roomGiftCloseRetryQueue(roomclose_effects.go:13) 同签名单方法接口冗余，可删用共享类型。其余全干净（接口=存储/RPC/Redis/外部边界缝；nil 守卫=值未找到/可选协议字段/构造期 fail-fast；overflow=join barrier 有界缓冲降级）。未逐行深读 mysqlstore 其余~10k SQL 与 logic 6850 行 handler（结构上最不藏投机型过度，电池干净）。结论：过度实现高度集中在主动建设型的 Stream 链路，存量代码贴需求、瘦。提示词运行正常；首轮 finding 自身准确性偏差已自更正，非提示词问题。**【06-15 复核】真实残留全部消除：emptyRoomGiftCloseRetryQueue 冗余接口 → ac31d3d0 已删（复用共享 roomGiftCloseRetryQueue）；注释失真（node_registry.go:74 / dispatch_stream.go:206）→ 3bcd9598 已改正；DeleteNodeStream 孤立死代码 → 3bcd9598 加注释明确为未来 multi-gateway 预留 seam（按文档化缝口径保留）。✅ 全部已处理。** |
| mode-entry-map | 2026-06-22 | -（地图模式不报告问题） | 06-22 增量刷新 `.local/review/entry-map.md`：基线从 2026-06-10 `05cf127f` 补到当前 HEAD `087c2fb7`。新增/校准内部 `/anchor/guild-binding/change`、RPC `ChangeAnchorGuildBinding`、`bootstrap-anchor-guild-bindings`、`scripts/dev/up.sh <dev|test|prod>`、`run-anchor-demo.sh`/`seed-anchor.sh`，并修正 anchor earning pipeline 步骤描述。覆盖性审查未新增确认 finding；新增入口分别已落入 money/release-ops/perf-capacity/security-authz 等现有主题覆盖。 |
| mode-architecture-review | 2026-06-16（A+D+C+B 同日四轮，新一轮四维全覆盖） | -（架构模式不按 P 分级；A 2 条 + D 2 条 + C 0 条 + B 1 条行动项，均文档/账本/流程级） | **维度 A**（`arch-reports/2026-06-16.md`）：主发现=7a77ba85 durable Stream 派发链路+节点注册，已建成但默认休眠在 `Dispatch.TransportMode` 后（默认 pubsub），北极星 #1/#5 一致、over-impl 已验证单实例价值，链路本体不动；缺口=shadow→stream cutover 无书面标准（rollout 文档已删）→ 账本新增 Dispatch 传输形态条目。4 条旧决策全维持。地图基线更新。**维度 D**（同报告增补）：勘误——A 节误称"CLAUDE.md 落位小节已合入"，git 证实 CLAUDE.md 自 06-10 零提交、从未合入，账本已更正；D1 06-10 落位规则行动项实际未执行（补 CLAUDE.md 降为可选）；D2 外呼超时逐个散设无统一收口（仅 4 个不值得抽工厂）；D3 not-found 收敛+删 svc 适配器正向。**维度 C**（同报告增补）：整体"维持现状"无代码行动项。C1 ticker 本期补 per-round deadline（默认 1min）关闭"挂住停调度"，但统一无 per-job override 对长结算是潜在失败模式（幂等可重跑兜底，条件进账本）；C2 Redis Stream durable 派发失败模式（trim/PEL/dedup/replay）逐项已处理，维持；C3 仓库无外部 MQ、3 个 consumer 是休眠 stub，激活默认 Redis Stream 防双消息技术漂移。**维度 B**（同报告增补）：B1（headline）service/ 是北极星未承认的业务层，gift 编排双轨——主送礼在 service/gift/service.go、私聊在 application/gift，互不调用、分别被不同 logic 入口用；无状态机制（combo/lucky/banner）合法属 service/；方向=北极星/CLAUDE.md 补界定 service/ 层 + 明确新 gift 编排落 application（文档级），是 D1 落位规则未写在 gift 域的实例；B2 room-core 上帝文件维持（check_rong 613≈618，本期仅重命名）；B3 adapter/+signaldispatch/ 落位合理。账本新增「service/ 层定位 + gift 编排双轨」条目。**提示词回改**：D1 勘误根因"凭记忆复述旧决策完成态"，已在 `mode-architecture-review.md` 证据纪律新增"完成态断言须附文件/grep 证据"（用户确认回改）。**四维新一轮全覆盖（A/B/C/D 全到 06-16）**，活跃决策条目增至 6 条、待补信息 3 条。基线评审 06-10：建 `_ledger.md` + 报告，四维全覆盖 |

## v1 → v2 映射

| v1 文件 | 去向 |
|---|---|
| bug-concurrency + bug-resource-leak | `concurrency-resource.md` |
| bug-error-swallow + bug-nil-boundary | `error-boundary.md` |
| consistency-state-machine + consistency-mysql-redis-drift | `consistency-state.md` |
| money-precision | `money-settlement.md`（扩充结算重跑专项） |
| security-credential-leak + compliance-privacy | `security-exposure.md` |
| perf-timeout-retry + security-rate-limit | `resilience-overload.md` |
| contract-api-drift + contract-field-semantics + contract-schema-alignment | `contract-alignment.md` |
| debt-dead-code(原文件损坏) + debt-duplication + debt-abstraction | `debt-cleanup.md` |
| release-compat + ops-tools-scripts | `release-ops.md` |
| 其余同名文件 | 原地重写（旧版在 `archive-v1/`） |
